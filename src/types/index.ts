/**
 * TypeScript type definitions
 * 
 * Shared types and interfaces used across the application.
 * Add exports here as types are created.
 */

// Example: export type { User, UserRole } from './user';
// Example: export type { ApiResponse } from './api';

