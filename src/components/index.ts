/**
 * Components barrel export
 * 
 * Re-exports all component categories for convenient importing.
 */

// Section components (hero, mission, roles, cta)
export * from './sections';

// Layout components (header, footer, page wrapper)
export * from './layout';

// Shared/common components
export * from './shared';

// Note: UI components (shadcn/ui) should be imported directly from @/components/ui/

